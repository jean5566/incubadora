VITE_API_URL=http://localhost:8000
# El backend Laravel corre en un puerto/servidor aparte (php artisan serve),
# distinto del servidor de Vite (npm run dev), así que hay que apuntar
# explícitamente a su URL.
